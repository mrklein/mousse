// mousse: CFD toolbox

FoamFile
{
  version     2.0;
  format      ascii;
  class       dictionary;
  object      probes;
}

probes
{
  fields ( p U );
  probeLocations
  (
    (0 0 0)
  );
  #includeEtc "case_dicts/post_processing/probes/probes.conf"
}

// vim: set ft=foam et sw=2 ts=2 sts=2:
